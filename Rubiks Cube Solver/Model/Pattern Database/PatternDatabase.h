#ifndef RUBIKS_CUBE_SOLVER_PATTERNDATABASE_H
#define RUBIKS_CUBE_SOLVER_PATTERNDATABASE_H

/*
This line declares a template class named PatternDatabaseEstimate with a template parameter T. 
This means the class can be instantiated with any data type T.
*/
template<typename T>
class PatternDatabaseEstimate
{
private:

public:
    int getEstimate(T rubiksCube)
    {
        return 0;
    }
};

#endif